<Window x:Class="ItemChecker.MVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:ItemChecker.MVVM.View"
        xmlns:viewModel="clr-namespace:ItemChecker.MVVM.ViewModel"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DataContext="{d:DesignInstance Type=viewModel:MainViewModel}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource Background}"
        TextElement.FontWeight="Normal"
        TextElement.FontSize="12"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="ItemChecker" 
        Height="710" Width="1300"
        MinHeight="540" MinWidth="955"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Closing="Window_Closing">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:HomeViewModel}">
            <view:BuyOrderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ParserViewModel}">
            <view:ParserView />
        </DataTemplate>
    </Window.Resources>
    <Border BorderBrush="DimGray" BorderThickness="0.5">
        <Grid Margin="10 0 10 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <!--top-->
            <Grid Grid.Row="0"
                  Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="190"/>
                </Grid.ColumnDefinitions>
                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Grid.Column="0">
                    <Image Source="../../Resources/ico.png" Height="18" Margin="1"
                           MouseLeftButtonDown="Image_MouseDown">
                        <Image.ContextMenu>
                            <ContextMenu Style="{StaticResource MaterialDesignContextMenu}">
                                <MenuItem Header="Links">
                                    <MenuItem Header="TrySkins"/>
                                    <Separator/>
                                    <MenuItem Header="Cs.Money"/>
                                    <Separator/>
                                    <MenuItem Header="Inventory"/>
                                    <MenuItem Header="SteamMarket"/>
                                </MenuItem>
                                <MenuItem Header="OpenFolder" Command="{Binding OpenFolderCommand}"/>
                                <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
                            </ContextMenu>
                        </Image.ContextMenu>
                    </Image>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" 
                        Click="Home_Click">
                        <TextBlock Text="Home"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" 
                        Click="Parser_Click">
                        <TextBlock Text="Parser"/>
                    </Button>
                </StackPanel>
                <Grid Background="Transparent" Grid.Column="1"  MouseLeftButtonDown="Window_DragMove"/>
                <StackPanel Grid.Column="2" Margin="0 0 -10 0"
                            HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="BETA" VerticalAlignment="Center"/>
                    <Button Height="25" Style="{StaticResource MaterialDesignToolForegroundButton}" Click="Calc_Click">
                        <materialDesign:PackIcon Kind="Calculator"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignToolForegroundButton}">
                        <materialDesign:PackIcon Kind="Bell"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignToolForegroundButton}" Click="Set_Click">
                        <materialDesign:PackIcon Kind="Settings"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" Click="MinWin_Click">
                        <materialDesign:PackIcon Kind="Minus"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" Click="Close_Click">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </StackPanel>
            </Grid>            
            <!--body-->
            <ContentControl Grid.Row="1" x:Name="bodyContent" Content="{Binding}" Margin="10" />
            <Border VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 25 0 0"
                    Height="150" Width="500" Background="WhiteSmoke" Visibility="Hidden"
                    BorderBrush="LightSlateGray" BorderThickness="0.5">
            </Border>
            <!--footer-->
            <Grid Grid.Row="2" VerticalAlignment="Center"
                  DataContext="{Binding MainInfo}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="User: {0} | Balance: {1}₽ / {2}₽">
                            <Binding Path="User"/>
                            <Binding Path="Balance" />
                            <Binding Path="BalanceCsm" />
                        </MultiBinding>
                    </TextBlock.Text>
                    <TextBlock.ToolTip>
                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="Course: {0}₽ | Balance: {1}$ / {2}$">
                                    <Binding Path="Course" />
                                    <Binding Path="BalanceUsd" />
                                    <Binding Path="BalanceCsmUsd" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </TextBlock.ToolTip>
                </TextBlock>
                <VirtualizingStackPanel Grid.Column="1" Orientation="Horizontal"
                                        HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrencyString, StringFormat='{}{0} | Steam Status:'}" Margin="0 0 2 0"/>
                    <TextBlock >
                        <materialDesign:PackIcon Kind="{Binding StatusCommunity}"/>
                    </TextBlock>
                </VirtualizingStackPanel>
            </Grid>
            <materialDesign:Snackbar Grid.Row="1" MessageQueue="{Binding Message}"/>
        </Grid>
    </Border>
</Window>
