<Window x:Class="ItemChecker.MVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:ItemChecker.MVVM.View"
        xmlns:viewModel="clr-namespace:ItemChecker.MVVM.ViewModel"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DataContext="{d:DesignInstance Type=viewModel:BuyOrderViewModel}"
        TextElement.Foreground="{StaticResource MaterialDesignBody}"
        Background="{StaticResource MaterialDesignLightBackground}"
        TextElement.FontWeight="Normal"
        TextElement.FontSize="12"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="ItemChecker" 
        Height="710" Width="1300"
        MinHeight="540" MinWidth="955"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Closing="Window_Closing">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:BuyOrderViewModel}">
            <view:BuyOrderView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ParserViewModel}">
            <view:ParserView DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    <Border BorderBrush="DimGray" BorderThickness="0.5">
        <Grid Margin="10 0 10 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" x:Name="TitleBar" Background="WhiteSmoke">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Grid.Column="0">
                    <Image Source="../../Resources/icon.png" Height="18" Margin="2">
                        <Image.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="OpenFolder" Command="{Binding OpenFolderCommand}"></MenuItem>
                                <MenuItem Header="Restart"></MenuItem>
                                <MenuItem Header="Exit" Command="{Binding ExitCommand}"></MenuItem>
                            </ContextMenu>
                        </Image.ContextMenu>
                    </Image>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" 
                        Click="Home_Click">
                        <TextBlock Text="MyOrders"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" 
                        Click="Parser_Click">
                        <TextBlock Text="Parser"/>
                    </Button>
                </StackPanel>
                <StackPanel Margin="0 0 -10 0" HorizontalAlignment="Right" Orientation="Horizontal" Grid.Column="1">
                    <Button Height="25" Style="{StaticResource MaterialDesignToolButton}">
                        <materialDesign:PackIcon Kind="BellNotification"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignToolButton}" Click="Setting_Click">
                        <materialDesign:PackIcon Kind="Settings"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" Click="MinWin_Click">
                        <materialDesign:PackIcon Kind="Minus"/>
                    </Button>
                    <Button Height="25" Style="{StaticResource MaterialDesignFlatButton}" Click="Close_Click">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </StackPanel>
            </Grid>
            <!--body-->
            <ContentControl Grid.Row="1" Content="{Binding}" Margin="10" />
            <!--/body-->
            <Grid Grid.Row="2" x:Name="StatusBar" VerticalAlignment="Center" Background="WhiteSmoke">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Account: {2} | Balance: {0}₽ / {1}₽">
                            <Binding Path="Balance" />
                            <Binding Path="BalanceCsm" />
                            <Binding Path="AccountName"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Margin="0 0 5 0">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Availability: {0}/{1}">
                                <Binding Path="Overstock" />
                                <Binding Path="Unavailable" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock x:Name="steamStatusOk">                    
                    <materialDesign:PackIcon Kind="{Binding StatusCommunity}"/>
                        <TextBlock.ToolTip>
                            <ToolTip Content="Steam Status"/>
                        </TextBlock.ToolTip>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
